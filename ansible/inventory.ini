# Static inventory. Host names match the Terraform container names, and Ansible
# connects via the Docker connection plugin (docker exec) - no SSH required.

[loadbalancer]
lb

[webservers]
web-1
web-2

[database]
db

[all:vars]
ansible_connection=community.docker.docker
ansible_python_interpreter=/usr/bin/python3
