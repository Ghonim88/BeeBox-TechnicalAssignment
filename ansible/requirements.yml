---
# Collections needed by the playbook. Install with:
#   ansible-galaxy collection install -r requirements.yml
collections:
  - name: community.docker   # docker connection plugin (configure containers without SSH)
  - name: community.mysql    # mysql_db / mysql_user modules for the database role
